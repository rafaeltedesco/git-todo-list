# Git - Todo List
---

Git commands utilizados:
```bash
git config --global user.name
git config --global user.email
git config --list
```

---

Gh commands utilizados:
```bash
gh auth login - autenticar
gh repo create - criar repo
```